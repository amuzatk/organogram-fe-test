// module.exports = {
//   testEnvironment: 'jsdom',
//   setupFilesAfterEnv: ['./setupTests.js'], // Adjust the path if necessary
// };


// module.exports = {
//   testEnvironment: 'jsdom',
//   setupFilesAfterEnv: ['./jest.setup.js'], // Adjust the path if necessary
// };

module.exports = {
  testEnvironment: 'jsdom',
  setupFilesAfterEnv: ['./jest.setup.js'], // Adjust the path if necessary
};
